<% const normalizedTitle = String(itemTitle || '').toLowerCase(); %>
<% const dungeon = allDungeons.find(d => d.name.toLowerCase() === normalizedTitle); %>
<% if (dungeon) { %>
    <%- await createDungeonTemplate(dungeon) %>
<% } else { %>
    <p>Dungeon not found: <%= itemTitle %></p>
<% } %>